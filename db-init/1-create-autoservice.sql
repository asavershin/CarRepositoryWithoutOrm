CREATE TABLE autoservice (
                     autoservice_id serial PRIMARY KEY,
                     autoservice_address varchar(50) NOT NULL,
                     autoservice_country varchar(50) NOT NULL,
                     autoservice_name varchar(50) NOT NULL
);